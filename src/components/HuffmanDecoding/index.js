import React from 'react';
import Styles from './styles.module.css';

const HuffmanDecoding = (props) => {
  console.log(props);
  
  return (
    <div className={Styles.HuffmanDecoding}>
      HuffmanDecoding Component
    </div>
  )
};

export default HuffmanDecoding;
